# Technical Notes Rules

## Purpose

このリポジトリは、ユーザーの疑問から作る解説ノートと、後で参照したい外部資料URLを、読み返しやすい日本語の知識インデックスとして蓄積するためのものです。

## Knowledge index

- `index.html` を外部資料と自作解説ノートの共通一覧として扱う。
- 一覧の各項目には、種類、タイトル、カテゴリ、ひとこと要約、追加日または確認日、検索用キーワードを入れる。
- 種類は原則として次の2つに分ける。
  - `解説ノート`: ユーザーの疑問から作った、このリポジトリ内のHTMLページ。
  - `外部資料`: URL、カテゴリ、ひとこと要約を管理するための外部ページへのリンク。
- 外部資料としてURLを渡された場合は、原則として個別の要約ページを作らず、`index.html` の一覧へカードを追加する。
- 外部資料カードは外部URLへ直接リンクし、タイトル、提供元が分かる情報、媒体属性、カテゴリ、ひとこと要約、確認日を載せる。
- 外部資料の媒体属性は、`data-source` と `.source-label` で管理する。例: `Zenn`、`Qiita`、`公式ドキュメント`、`企業ブログ`、`個人ブログ`、`GitHub`、`論文`。

## Note creation

- ユーザーの入力が `質問:` または `質問：` で始まる場合だけ、`.agents/skills/write-technical-note/SKILL.md` に従って解説ノートを作る。
- 解説ノートは1テーマにつき `notes/<yyyy-mm-dd>-<short-slug>/index.html` を作る。
- 作成した解説ノートも `index.html` の共通一覧に追加する。
- 既存の `styles/site.css` とページ構造を再利用する。
- ビルドツール、フレームワーク、外部 JavaScript 依存は追加しない。

## Research and accuracy

- 技術仕様は公式ドキュメント、標準仕様、原論文などの一次情報を優先する。
- 時点で変わりうる内容は調査日を明記する。
- 推測を事実として書かない。不確かな点は明示する。
- 解説ノートの末尾には参照 URL と確認日を載せる。
- 外部資料カードには確認日を載せ、ページ内容を読んでいない場合は「未読」「要確認」など事実に合う短いメモにする。

## Publish flow

- 解説ノート作成または外部資料登録を行った場合は、検証後に対象ファイルだけを commit し、`main` へ push する。
- commit / push 前に必ず次を確認する。
  - `git status -sb` で作業対象外の変更が混ざっていないこと。
  - `git diff --check` で空白エラーがないこと。
  - 追加・変更した内部リンク、外部リンク、相対パスに明らかな誤りがないこと。
  - 秘密情報、個人情報、社内情報、未公開 URL、ローカル環境固有の値が含まれていないこと。
- `git add` は対象ファイルを明示して行う。`git add -A` は使わない。
- `.codex-remote-attachments/` など、ユーザー添付由来の未追跡ファイルは commit しない。
- 次の場合は commit / push せず、理由をユーザーへ報告する。
  - 作業対象外の差分があり、切り分けができない。
  - 公開してよいか判断できない情報が含まれている。
  - 検証でリンク切れ、HTML構造の破綻、重大な表示崩れが見つかった。
  - 調査内容に未解決の不確かさがあり、公開すると誤解を招く。

## Safety

- 個人情報、認証情報、秘密鍵、社内情報、未公開 URL を書かない。
- ユーザーのローカルパス、アカウント名、環境変数の値を記事に含めない。
- 解説ノート作成または外部資料登録以外の変更は、ユーザーから明示的に依頼された場合だけ commit / push する。

## Verification

- 追加・変更したリンクと相対パスを確認する。
- HTML の主要タグが閉じていることを確認する。
- 可能ならローカルサーバーでトップページと追加ページを表示確認する。
