# Technical Notes Rules

## Purpose

このリポジトリは、ユーザーの疑問を調査し、読み返しやすい日本語の静的 HTML ノートとして蓄積するためのものです。

## Note creation

- ユーザーの入力が `質問:` または `質問：` で始まる場合、`.agents/skills/write-technical-note/SKILL.md` に従う。
- 1テーマにつき `notes/<yyyy-mm-dd>-<short-slug>/index.html` を作る。
- `index.html` のカード一覧にも新しいノートを追加する。
- 既存の `styles/site.css` とページ構造を再利用する。
- ビルドツール、フレームワーク、外部 JavaScript 依存は追加しない。

## Research and accuracy

- 技術仕様は公式ドキュメント、標準仕様、原論文などの一次情報を優先する。
- 時点で変わりうる内容は調査日を明記する。
- 推測を事実として書かない。不確かな点は明示する。
- 各ノートの末尾に参照 URL と確認日を載せる。

## Safety

- 個人情報、認証情報、秘密鍵、社内情報、未公開 URL を書かない。
- ユーザーのローカルパス、アカウント名、環境変数の値を記事に含めない。
- 自動で push、公開、デプロイしない。コミットもユーザーから明示的に依頼された場合だけ行う。

## Verification

- 追加・変更したリンクと相対パスを確認する。
- HTML の主要タグが閉じていることを確認する。
- 可能ならローカルサーバーでトップページと追加ページを表示確認する。
